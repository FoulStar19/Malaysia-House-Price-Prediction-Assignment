# Copy this file to .streamlit/secrets.toml and fill in your deployed
# backend URL. Not needed for local dev (defaults to http://localhost:8000).
# secrets.toml itself is gitignored - don't commit real secrets.

BACKEND_URL = "http://127.0.0.1:8000"
